name: Binance USD-M Cross-Asset Research Data

on:
  workflow_dispatch:
  pull_request:
    paths:
      - scripts/download_binance_usdm_cross_asset_data.py
      - .github/workflows/binance-usdm-cross-asset-data.yml
  push:
    branches:
      - agent/long-lived-research-cycle

permissions:
  contents: read

jobs:
  fetch-cross-asset-data:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'pull_request' ||
      contains(github.event.head_commit.message, '[fetch-cross-asset-data]')
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install data dependencies
        run: python -m pip install "pandas>=2.2" "requests>=2.32" "pyarrow>=17"

      - name: Compile downloader
        run: python -m py_compile scripts/download_binance_usdm_cross_asset_data.py

      - name: Download and freeze verified archives
        run: |
          python scripts/download_binance_usdm_cross_asset_data.py \
            --symbols BNBUSDT XRPUSDT ADAUSDT DOGEUSDT LINKUSDT LTCUSDT BCHUSDT AVAXUSDT \
            --start-month 2020-01 \
            --end-month 2026-06 \
            --daily-start 2026-07-01 \
            --daily-end 2026-07-21 \
            --workers 16 \
            --output artifacts/binance_usdm_cross_asset_data

      - name: Validate manifest and package contents
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          symbols = (
              'BNBUSDT', 'XRPUSDT', 'ADAUSDT', 'DOGEUSDT',
              'LINKUSDT', 'LTCUSDT', 'BCHUSDT', 'AVAXUSDT',
          )
          root = Path('artifacts/binance_usdm_cross_asset_data')
          manifest = json.loads((root / 'manifest.json').read_text(encoding='utf-8'))
          required = {
              f'{symbol}_{kind}'
              for symbol in symbols
              for kind in ('klines', 'fundingRate')
          }
          missing = sorted(required - set(manifest['datasets']))
          if missing:
              raise SystemExit(f'missing datasets: {missing}')
          for name, info in manifest['datasets'].items():
              path = Path(info['path'])
              if not path.exists() or path.stat().st_size <= 0:
                  raise SystemExit(f'invalid dataset file: {name} {path}')
          print(json.dumps({
              'datasets': len(manifest['datasets']),
              'archives': len(manifest['archives']),
              'missing_archives': len(manifest['missing_archives']),
          }, indent=2))
          PY

      - name: Upload immutable cross-asset snapshot
        uses: actions/upload-artifact@v4
        with:
          name: binance-usdm-cross-asset-${{ github.sha }}
          path: artifacts/binance_usdm_cross_asset_data
          if-no-files-found: error
          compression-level: 0
          retention-days: 30
