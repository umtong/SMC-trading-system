name: Build Causal Microstructure Bundle macOS

on:
  push:
    branches:
      - "research/data-microstructure-bundle-macos-20260724"
    paths:
      - "research/build_causal_microstructure_bundle.py"
      - ".github/workflows/build-causal-microstructure-bundle-macos.yml"
      - "research/MICROSTRUCTURE_BUNDLE_MACOS_TRIGGER.txt"
  pull_request:
    branches:
      - main
    paths:
      - "research/build_causal_microstructure_bundle.py"
      - ".github/workflows/build-causal-microstructure-bundle-macos.yml"
      - "research/MICROSTRUCTURE_BUNDLE_MACOS_TRIGGER.txt"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-causal-microstructure-bundle-macos
  cancel-in-progress: false

jobs:
  build:
    runs-on: macos-14
    timeout-minutes: 300
    env:
      HF_HUB_DOWNLOAD_TIMEOUT: "300"
      HF_HUB_ETAG_TIMEOUT: "120"
      HF_HUB_ENABLE_HF_TRANSFER: "0"
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/data-microstructure-bundle-macos-20260724
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: python -m pip install --upgrade pip "pandas<3" pyarrow huggingface_hub

      - name: Restore partial Hugging Face cache
        uses: actions/cache@v4
        with:
          path: research/microstructure_bundle/.hf_cache
          key: causal-btc-microstructure-source-2023-2026-v2

      - name: Build future-column-free bundle
        run: |
          mkdir -p research/microstructure_bundle
          python research/build_causal_microstructure_bundle.py \
            --start 2023-01-01 \
            --end 2026-05-31 \
            --out research/microstructure_bundle \
            2>&1 | tee research/microstructure_bundle_build.log

      - name: Audit forbidden columns and outputs
        run: |
          test -s research/microstructure_bundle/COMPLETE
          python - <<'PY'
          import json, re
          from pathlib import Path
          manifest = json.loads(Path('research/microstructure_bundle/manifest.json').read_text())
          forbidden = re.compile(manifest['forbidden_pattern'], re.I)
          assert len(manifest['outputs']) >= 4
          assert sum(item['rows'] for item in manifest['outputs']) > 250000
          for item in manifest['outputs']:
              assert not [column for column in item['columns'] if forbidden.search(column)]
              assert item['bytes'] < 95_000_000
          print(json.dumps(manifest['outputs'], indent=2))
          PY

      - name: Commit immutable bundle
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f research/microstructure_bundle research/microstructure_bundle_build.log
          if git diff --cached --quiet; then
            echo "No bundle change"
          else
            git commit -m "Persist causal BTC microstructure research bundle"
            git pull --rebase origin research/data-microstructure-bundle-macos-20260724
            git push origin HEAD:research/data-microstructure-bundle-macos-20260724
          fi

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: causal-microstructure-bundle-macos-${{ github.run_id }}
          path: |
            research/microstructure_bundle
            research/microstructure_bundle_build.log
          if-no-files-found: warn
          retention-days: 30
